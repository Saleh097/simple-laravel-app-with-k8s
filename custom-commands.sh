kubectl run nginx --image=nginx --port=80  # run a quick deployment

cat /etc/os-release  # see linux distro

kubectl port-forward <podname> <portnumber>   # forward traffic form global port to pod port

composer create-project laravel/laravel example-app  # fresh laravel project

zip -r <zip file name> <directory name>  # zip a directory

minikube service s<service name>  # set external service 